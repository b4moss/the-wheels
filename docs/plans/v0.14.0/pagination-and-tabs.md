# ページネーション / Tabs 改修

- **状態**: 意図スタブ
- **マイルストーン**: `v0.14.0`

テーマは別々。どちらも載せ替えでよく出す。Tabs 改修の範囲は実装前に狭く切る（最小実装の延長に留める）。

## ページネーション

よく使う標準的なページ番号・件数 UI。

- InfiniteScroll（連続追加）とは役割を分ける
- 詳細仕様は実装前に切る

## Tabs 改修

現行は最小実装。ステップナビ（同版: [expandable-and-step-nav.md](./expandable-and-step-nav.md)）は段階表示専用で `TwTabs` を使わない。本項目の Tabs 改修はそれとは独立に、最小実装の延長として狭く切る。

## 含まないもの

- 無限スクロールの置き換え
- a11y 本検討

----

以上
